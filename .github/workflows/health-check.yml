name: Daily health check

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          # Admin PAT so the write-back push bypasses branch protection
          # (enforce_admins is off, so admin pushes are allowed direct to master).
          token: ${{ secrets.HEALTH_CHECK_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run health check
        run: node scripts/health-check.js

      - name: Commit updated status
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/apis.json data/history.json
          git diff --cached --quiet || git commit -m "chore: daily health check [skip ci]"
          git push
